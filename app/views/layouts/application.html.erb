<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnlyRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav>
        <ul>
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "Members Only", members_only_path %></li>
          <li><%= link_to "Sign Up", new_user_registration_path unless user_signed_in? %></li>
          <li><%= link_to "Sign In", new_user_session_path unless user_signed_in? %></li>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete if user_signed_in? %></li>
        </ul>
      </nav>
    </header>

    <div class="content">
      <%= yield %>
    </div>

    <footer>
      <p>&copy; <%= Time.now.year %> Members-Only Rails | Created by CyberRacc</p>
    </footer>
  </body>

</html>
